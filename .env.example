# GitHub PAT with `read:user` and `public_repo` scopes.
# Used for public repo listing, LOC walk and stats.
ACCESS_TOKEN=

# Optional. HMAC key used to hash repo names before they are written to
# the committed loc_cache.json file. Only meaningful when private repos
# are included. Kept for compatibility.
CACHE_HMAC_KEY=
